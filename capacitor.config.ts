import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'xtendtv_virtual_ticket_application',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
